#include <stdio.h>
#include <stdlib.h>

int main()
{
    int favNum = 90;
    char myChar = 'I';
    printf("My favorite %c is %d", myChar, favNum); // %d means to print out an integer %s means to print out a string %f means to print out a decimal %c means to print out a character

    return 0;
}