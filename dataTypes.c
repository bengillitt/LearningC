#include <stdio.h>
#include <stdlib.h>

int main()
{

    int age = 40; // Whole numbers
    double gpa = 3.7; // Decimal Numbers
    char grade = 'A'; // A single character
    char phrase[] = "Giraffe Academy"; // Creates a string, array of characters

    return 0;
}